### Cilium ClusterMesh

* This is a work in progress, details might be incomplete
* the `cilium` binary is still required on a bastion host
* `cilium clustermesh connect` requires two flags, `--allow-mismatching-ca` and `--helm-release-name rke2-cilium`

More notes to follow...

---
